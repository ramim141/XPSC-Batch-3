#include <bits/stdc++.h>
using namespace std;

int main()
{
    int s, e;
    cin >> s >> e;
    int cnt = 0;
    for (int i = s; i <= e; i++)
    {
        cnt++;
    }
    cout << cnt;

    return 0;
}